export interface Address {
  id: string;
  street: string;
  number: string;
  city: string;
  state: string;
  country: string;
  zip_code: string;
  // Timestamps
  created_at: string;
  updated_at: string;
}
